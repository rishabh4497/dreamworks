/**
 * Centralized model identifiers — bump these to roll the whole app onto a new
 * model, then bump every prompt module's `promptVersion` so the cache invalidates.
 */
export const MODEL_FLASH_LITE = "gemini-3.1-flash-lite";
export const MODEL_FLASH_REASONING = "gemini-2.5-flash";
